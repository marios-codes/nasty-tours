//- We simply declare the class with a dot
header.header  
    //- Two classes, one following the other with a dot for each class
    nav.nav.nav--tours  
        a.nav__el(href='#') All tours
    //- When comes to div there is no need to write it, just the dot(.) and class(es)
    .header__logo 
        img(src='/img/logo-white.png' alt='Natours logo')
    nav.nav.nav--user 
        //- a.nav__el(href='#') My Bookings 
        //- a.nav__el(href='#')
        //-     img.nav__user-img(src='img/user.jpg' alt='User photo')  
        //-     span Jonas
        button.nav__el Log in
        button.nav__el.nav__el--cta Sign up